# 📋 최근 업데이트 및 버그 수정 내역 (Recent Updates & Bug Fixes)

이 문서는 최근 시스템 고도화 및 시뮬레이션 안정성 확보를 위해 수행된 주요 기능 개발, 오류 수정 및 검증 세부 내용을 정리한 문서입니다.

---

## 📌 1. 일자 전환(Day Transition) 워크플로우 구현

* **배경**: 물류창고 일자별 출고 프로세스 중, 당일 날짜(`route_zone`)의 모든 물량이 출고되면 자동으로 일정이 멈추고 다음 영업일로 전환할 수 있는 제어 체계가 필요했습니다.
* **구현 내용**:
  * **일일 마감 자동 감지 및 보고서 작성**: 관제탑 노드(`control_tower_node.py`)가 데이터베이스의 당일 출고 대상 패키지 상태를 실시간 확인하여 남은 수량이 0이 되는 시점에 마감 이벤트(`[Day Finished]`)를 발생시킵니다. 이후 포장 완료 통계를 집계하여 `daily_report_YYYY-MM-DD.md` 마크다운 보고서를 자동으로 작성 및 저장합니다.
  * **Redis 제어 상태 전이**: Redis 캐시의 `system:day_status` 상태를 `'PENDING_TRANSITION'`으로 전환하고 `system:completed_day`에 마감 날짜를 기록하여 스케줄러 루프를 일시 중지(Sync Hold) 모드로 전환합니다.
  * **대시보드 UI 연동**: 웹 대시보드 상단에 일자 마감 및 다음 날짜 대기 배너(`Next Day Transition` 활성 버튼)가 실시간 표시되도록 연동하고, 버튼 클릭 시 `/api/start_next_day` POST 요청을 통해 다음 영업일을 개시할 수 있도록 연동했습니다.

---

## 🖥️ 2. 웹 대시보드 2D 맵 UI 격자 개편

* **배경**: 기존 캔버스(Canvas) 기반의 단순 2D 좌표 표시 방식은 물류창고의 실제 물리 공간 구조와 대칭형 컨베이어 라인의 점유 상태를 직관적으로 파악하기 어려웠습니다.
* **구현 내용**:
  * 캔버스 렌더링 방식에서 **HTML Grid/Flexbox 기반의 반응형 플로어 플랜 격자 레이아웃**으로 레이아웃을 개편했습니다.
  * 상단 주차 구역(스팟 `spot_01`~`spot_10`), 좌우 대칭형 컨베이어 인바운드 라인 1~3(A/B구역), 중앙 AMR 주행 공간, 하단 포장 아웃바운드 라인(A/B구역)의 물리 격자를 완전히 시각화했습니다.
  * `floor_qr_map` 및 각 스팟의 점유 상태(`OCCUPIED`/`EMPTY`)에 따라 실시간(1Hz)으로 배경 색상(점유 시 시안색 테두리 및 라벨 표시)과 상세 정보 툴팁이 활성화되도록 JS 매핑 로직을 업데이트했습니다.

---

## 🔄 3. 중복 입고 검사 오류 및 무한 루프 버그 해결

* **배경**: 수령인 이름(`customer_name`) 기반으로 중복 입고 여부를 검증하다 보니, 과거에 이미 포장 완료된 수령인의 기록이 남아 있는 경우 신규 입고된 동일 수령인의 물품을 중복 입고로 오인하여 직송 처리로 빠지게 만들었습니다. 또한, 시뮬레이터에서 직송 지시를 받았을 때 상태 전이가 유실되어 무한 적재 요청 루프에 빠지는 교착이 발생했습니다.
* **구현 내용**:
  * **관제탑 (`control_tower_node.py`)**: `CheckWarehouseStatus` 서비스 호출 시 조회 기준을 `customer_name` 대신 고유 식별자인 `package_id` 기준으로 변경하여 과거 출고 데이터와 꼬이지 않도록 정밀화했습니다.
  * **시뮬레이터 (`run_full_simulation_robot.py`)**: 관제탑으로부터 직송 지시(`is_already_in_warehouse=True`)를 받았을 때, 데이터베이스 내 해당 패키지의 상태를 `IN_WAREHOUSE`로 즉시 업데이트하여 다음 루프의 적재 대상(WAITING)에서 올바르게 제외되도록 수정하여 무한 적재 시도 현상을 차단했습니다.

---

## 🔄 4. 포장 완료 후 180도 회전 시 중복 트리거(Double-trigger) 버그 수정

* **배경**: 포장 로봇이 4번째 슬롯 포장을 완료한 후, 2x4 배열 뒷열의 상자를 포장하기 위해 작업대를 180도 회전(`ROTATE_WORKSTATION`)시키는 AMR 태스크를 수행합니다. 제자리 회전 완료 후 작업대가 원래 자리(`sg2_out_00_A`)에 재진입할 때, 이송 완료 콜백(`workstation_move_completed_callback`)에서 도착지가 포장존이라는 조건 하나만 확인하여 포장 공정(`trigger_packaging_process`)을 중복 실행(Double-trigger)하는 치명적인 버그가 있었습니다.
* **구현 내용**:
  * **이송 인터페이스 수정**: 이송 완료 콜백 및 응답 처리 핸들러에 출발지(`start`) 정보를 추가로 파싱 및 전달하도록 로직을 연동했습니다.
  * **방어 조건 추가**: 최종 도착 완료 시, 출발지가 `sg2_out_00_A_ROTATING`이거나 `ROTATING` 문자열을 포함하는 제자리 회전 동작이었던 경우 포장 로봇 동작을 다시 트리거하지 않고 스킵하도록 예외 처리를 추가했습니다.
  * **자원 정합성 유지**: 4번째 슬롯 완료 시 `rotation_triggered` 세트에 작업대 ID를 임시 등록해두고, 최종 8번째 슬롯 포장 및 회수 처리가 끝나는 시점에 상태를 제거하여 다음 사이클의 정상 작동을 보장했습니다.

---

## 🛠️ 5. 기타 시스템 안정화 코드 반영

* **Redis 문자열 디코딩 오류 방지**: Redis 커넥션에서 `decode_responses=True` 설정을 사용할 경우, `redis_client.get('system:day_status')` 반환값이 이미 문자열(`str`)임에도 `.decode('utf-8')`를 호출하여 속성 오류가 발생하던 문제를 `isinstance(val, str)` 타입 검사를 통해 분기 처리하여 해결했습니다.
* **빈 작업대 검색 정합성 보장**: 대시보드 및 관제 노드에서 작업대 배치 시, 이미 슬롯에 패키지가 채워진 상태를 체크할 때 기존 `status = 'IN_WORKSTATION'` 상태만 검사하던 것에서 `IN_WAREHOUSE` 상태까지 통합 점검하도록 SQL을 수정하여, 창고 내 보관 중인 작업대가 중복 배정되거나 누수되는 현상을 완벽히 방지했습니다.
* **Docker 포트 충돌(8080) 해결**: 호스트 머신의 `8080` 포트가 다른 프로세스에 의해 이미 사용 중인 경우 `warehouse_adminer` 컨테이너 기동이 실패하는 문제를 해결하기 위해, Adminer의 호스트 바인딩 포트를 `8080`에서 **`8082`**로 변경하여 포트 충돌을 방지하고 컨테이너가 정상적으로 실행되도록 조치했습니다.
* **FastAPI 대시보드 포트 충돌(8000) 해결**: 호스트 머신에 구동 중인 NVIDIA Omniverse Nucleus Auth 서비스가 `8000` 포트를 이미 점유하고 있어 대시보드 서버(`dashboard_server.py`) 기동 시 `Address already in use` 오류가 발생하는 문제를 해결하기 위해, 대시보드의 실행 포트를 `8000`에서 **`8009`**로 변경하여 충돌을 회피했습니다.
* **AMR 액션 서버 무응답 시 DB 상태 롤백(Rollback) 처리**: 관제탑 노드가 실행되는 시점에 AMR 액션 서버(`mock_full_robot_node`)가 아직 켜지지 않았거나 요청이 거절/실패하는 경우, 이미 `MOVING_TO_...`로 바뀐 작업대 위치와 해제된 창고 스팟 상태가 원복되지 않아 교착 상태(Deadlock)에 빠지는 문제를 해결하기 위해 `recover_workstation_move_db_state()` 함수를 작성하여 실패 시 DB 상태를 완벽히 롤백하도록 개선했습니다.

---

## 🚀 6. 작동 검증 결과 요약

* 대용량 패키지 데이터셋(150개)을 이용해 전체 인바운드 적재 $\rightarrow$ Look-ahead 예비 작업대 호출 $\rightarrow$ 아웃바운드 포장 $\rightarrow$ 4번째 슬롯 회전 $\rightarrow$ 8번째 슬롯 완료 후 회수 $\rightarrow$ 다음 영업일 전환 시나리오를 반복 수행했습니다.
* **결과**: 이중 트리거 현상이 발생하지 않고, 작업대 회전 시 포장 로봇 대기 상태가 정확히 유지되었으며, 일자 마감 시 `daily_report_YYYY-MM-DD.md` 보고서가 정상 생성되고, 대시보드 버튼을 통해 스케줄러가 다음 날짜 데이터로 끊김 없이 복귀하는 흐름을 최종 확인 완료했습니다.
